*.iml
.gradle
/local.properties
.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
PULL_REQUEST.md

# Secrets (never commit)
# Note: local.defaults.properties is intentionally tracked — CI placeholder values only, no real keys
*.keystore
*.jks

# Build outputs
app/build/
*.apk
*.aab

# gstack local analytics
.gstack/

# Claude settings (local only)
.claude/settings.local.json
